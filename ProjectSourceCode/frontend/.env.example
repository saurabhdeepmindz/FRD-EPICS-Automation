# Frontend environment variables
# Copy this to .env.local for local development — never commit .env.local

# URL of the NestJS backend API (server-side and client-side)
NEXT_PUBLIC_API_URL=http://localhost:4000

# App port
PORT=3000
